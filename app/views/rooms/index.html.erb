<div class="grid grid-cols-12 mx-6">
  <div class="col-span-6 border border-black">
    <div class="">標題1</div>
  </div>
  <div class="col-span-6 flex justify-end border border-black">
    <div class="">標題2</div>
  </div>
</div>

<div class="grid grid-cols-12 mx-6">
  <div class="md:col-span-3 bg-gray-100 h-64">
    <div class="">
      <div class="mx-auto">user_name</div>
      <div class="h-32 bg-cover bg-center rounded-full" style="background-image: url(<% root_path %>/img/back_1.jpg)">1</div>
      <div class="">session_id</div>
      <div class="">show</div>
      <div class="">edit</div>
      <div class="">Destroy</div>   
    </div>
  </div>

  <div class="md:col-span-3 bg-gray-100 h-64">22</div>
  <div class="md:col-span-3 bg-gray-100 h-64">33</div>

</div>

<p id="notice"><%= notice %></p>

<div>Rooms</div>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Session</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= room.user.display_name%></td>
        <td><%= room.name %></td>
        <td><%= room.session_id %></td>
        <td><%= link_to 'Show', room %></td>
        <td><%= link_to 'Edit', edit_room_path(room) %></td>
        <td><%= link_to 'Destroy', room, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Room', new_room_path, params: current_user %>
